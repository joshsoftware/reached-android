<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.joshsoftware.reachedapp">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:name="com.joshsoftware.reachedapp.FamApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name="com.joshsoftware.reachedapp.ui.GroupWaitActivity"
            android:theme="@style/Theme.MyApplication"/>
        <activity
            android:name="com.joshsoftware.reachedapp.ui.MapActivity"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name="com.joshsoftware.reachedapp.ui.GroupMemberActivity"
            android:theme="@style/Theme.MyApplication" />
        <activity
            android:name="com.joshsoftware.reachedapp.ui.WearLoginActivity"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.joshsoftware.reachedapp.ui.WearGroupListActivity"
            android:theme="@style/Theme.MyApplication">

        </activity>

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDacw9al4jpEeuFw4UxOaPSU4ON7a_gMjE" />

        <service
            android:name="com.joshsoftware.core.service.LocationUpdateService"
            android:enabled="true"
            android:exported="true"/>
        <service android:name="com.joshsoftware.reachedapp.service.ReachedWearFirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>